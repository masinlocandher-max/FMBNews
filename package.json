{
  "name": "fmb-news",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/generate-daily-brief-recovery.mjs && node scripts/build.mjs",
    "verify": "node scripts/export-daily-brief-recovery-log.mjs",
    "test:mobile:browser": "node scripts/run-mobile-qa.mjs",
    "test:mobile": "npm run build && npm run verify && npm run test:mobile:browser",
    "deploy:cloudflare": "npm run build && npm run verify && npx wrangler deploy"
  }
}

name: Verify standalone FMB News

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enforce canonical deployment boundary
        shell: bash
        run: |
          set -euxo pipefail
          test -f vercel.json
          grep -q '"deploymentEnabled": false' vercel.json
          test ! -d .vercel
          grep -q 'www.francinemariebautista.com/news\*' wrangler.jsonc
          grep -q 'francinemariebautista.com/news\*' wrangler.jsonc
          ! grep -R -n '/fmbnews' src wrangler.jsonc
          ! grep -R -n -E 'masinlocandher-max/FMB-Ecosystem' src site public scripts package.json wrangler.jsonc

      - name: Build standalone newsroom
        run: npm run build

      - name: Verify canonical newsroom surfaces
        shell: bash
        run: |
          set -o pipefail
          npm run verify 2>&1 | tee verify.log

      - name: Preserve verifier diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: fmb-news-verify-log
          path: verify.log
          if-no-files-found: error
          retention-days: 3

      - name: Verify deployable mobile system
        shell: bash
        run: |
          set -euxo pipefail

          for page in index archive world explainer fmb-brief horoscope crossword about; do
            if [ "$page" = index ]; then file="dist/news/index.html"; else file="dist/news/$page/index.html"; fi
            test -f "$file"
            grep -q 'FMB News' "$file"
            grep -q 'FMB Worldwide' "$file"
            grep -q 'FMB Explainer' "$file"
            grep -q 'FMB Daily Brief' "$file"
            grep -E -q 'fmb-news-mobile-system\.css\?v=[0-9a-f]{10}' "$file"
            grep -q 'fmb-news-mobile-products.js?v=20260902-products-v3' "$file"
            ! grep -E -q '<link[^>]+fmb-news-mobile-(personalization|home|global|products|app-polish|home-live-hero|contrast-lock|product-heroes|menu-holder)\.css' "$file"
          done

          grep -q 'Read the Latest' dist/news/index.html
          grep -q '>Customize<' dist/news/index.html
          grep -q 'fmb-approved-hero-copy' dist/news/index.html
          grep -q 'fmb-approved-hero-ticker' dist/news/index.html
          grep -q '14fKTwMW0qnVi36eVSAjSBr05_VZgq4kf' scripts/fetch-approved-mobile-assets.mjs

          test -f dist/news/manifest.webmanifest
          test -f dist/news/sw.js
          test -f dist/news/assets/css/fmb-news-mobile-system.css
          test -f dist/news/assets/images/mobile/fmb-mobile-hero.jpg
          test -f dist/news/assets/images/mobile/fmb-daily-brief-mug.jpg
          test -f dist/news/assets/images/mobile/fmb-explainer-fallback.jpg
          test "$(stat -c%s dist/news/assets/images/mobile/fmb-mobile-hero.jpg)" -gt 20000
          test "$(stat -c%s dist/news/assets/images/mobile/fmb-daily-brief-mug.jpg)" -gt 20000
          test "$(stat -c%s dist/news/assets/images/mobile/fmb-explainer-fallback.jpg)" -gt 20000

          grep -q 'min-height:44px' dist/news/assets/css/fmb-news-mobile-personalization.css
          grep -q 'focus-visible' dist/news/assets/css/fmb-news-mobile-personalization.css
          grep -q 'One exact hero canvas' dist/news/assets/css/fmb-news-mobile-product-heroes.css
          grep -q 'height:300px' dist/news/assets/css/fmb-news-mobile-product-heroes.css

          grep -q 'Hindi hawak ng mga bituin ang ating kapalaran, meron tayong freewill gamitin natin' dist/news/horoscope/index.html
          grep -q '35+ current-event answers' dist/news/crossword/index.html
          ! grep -E -q 'Reveal Letter|Reveal Word|Reveal Puzzle|data-cw-reveal' dist/news/crossword/index.html
          ! grep -E -q 'Reveal Letter|Reveal Word|Reveal Puzzle|data-cw-reveal' dist/news/assets/js/fmb-news-weekly-crossword.js

          ! grep -R -E '>FMB Explained<' dist/news --include='*.html'
          ! grep -R -E 'nc-mobile-dock|fmb-app-dock|Mobile news navigation' dist/news --include='*.html'
          ! grep -R -E '(["'"'"'(=])\/assets\/' dist/news --include='*.html' --include='*.css' --include='*.js' --include='*.mjs'

          total_pages="$(find dist/news -type f -name index.html | wc -l | tr -d ' ')"
          system_pages="$(grep -rl -E 'fmb-news-mobile-system\.css\?v=[0-9a-f]{10}' dist/news --include='index.html' | wc -l | tr -d ' ')"
          product_js_pages="$(grep -rl 'fmb-news-mobile-products.js?v=20260902-products-v3' dist/news --include='index.html' | wc -l | tr -d ' ')"
          echo "coverage total=$total_pages system=$system_pages product_js=$product_js_pages"
          test "$total_pages" -eq "$system_pages"
          test "$total_pages" -eq "$product_js_pages"

          grep -q 'rel="manifest" href="/news/manifest.webmanifest"' dist/news/index.html
          grep -q 'serviceWorker.register' dist/news/assets/js/fmb-news-mobile-personalization.js
          grep -q 'news_push_subscriptions' dist/news/assets/js/fmb-news-mobile-personalization.js
          grep -q "addEventListener('push'" dist/news/sw.js
          grep -q 'notificationclick' dist/news/sw.js

      - name: Install mobile browser QA runtime
        run: |
          npm install --no-save --no-audit --no-fund playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Run complete mobile browser QA
        run: npm run test:mobile:browser
